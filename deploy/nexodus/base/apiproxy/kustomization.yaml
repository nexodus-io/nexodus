apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: apiproxy
    literals:
      - APIPROXY_OIDC_URL=https://auth.try.nexodus.local/realms/nexodus
      - APIPROXY_OIDC_BACKCHANNEL=https://auth:8443/realms/nexodus
      - APIPROXY_API_DOMAIN=api.try.nexodus.local
      - APIPROXY_WEB_DOMAIN=try.nexodus.local
      - APIPROXY_WEB_ORIGINS=https://try.nexodus.local

  - name: apiproxy-envoy-config
    files:
      - files/envoy.yaml
resources:
  - deployment.yaml
  - ingress.yaml
  - ingress-frontend.yaml
  - service.yaml
commonLabels:
  app.kubernetes.io/component: apiproxy
  app.kubernetes.io/instance: apiproxy
  app.kubernetes.io/name: apiproxy
